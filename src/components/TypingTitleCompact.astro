---
interface Props {
  size?: 'small' | 'medium';
}

const { size = 'small' } = Astro.props;
const fontSize = size === 'small' ? '24px' : '32px';
---

<span class="typing-title" style={`font-size: ${fontSize};`}>typing…</span>

<style>
  .typing-title {
    font-family: 'Jugendreisen', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    color: hsla(49, 100%, 72%, 1.0);
    text-shadow: 
      0 0 10px rgba(255, 229, 114, 0.4), 
      0 0 20px rgba(255, 229, 114, 0.2),
      0 0.015em 0 #FFBF45;
    font-weight: 400;
    white-space: nowrap;
  }
</style>